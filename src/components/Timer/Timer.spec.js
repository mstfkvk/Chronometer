/* eslint-disable no-unused-vars */
import React from "react";
import { shallow } from "enzyme";
import Timer from "./Timer";

describe("Timer Component Tests", () => {
  it("Check the title of the React app", () => {
      
      
  });
});
